<% provide :title, t(".all") %>
<h1><%= t ".all" %></h1>

<%= form_for :search do |f| %>
  <%= f.text_field :search_key, remote: true, class: "form-control search_box" %>
<% end %>

<%= will_paginate %>

<ul class="users-list">
  <%= render @users %>
</ul>

<%= will_paginate %>
